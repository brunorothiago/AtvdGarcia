spring:
  datasource:
    url: jdbc:h2:file:./data/cadastrodb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false

  h2:
    console:
      enabled: true
      path: /h2-console
